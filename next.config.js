/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Add your photo host domains here if you use a CDN.
    domains: []
  }
};

export default nextConfig;
